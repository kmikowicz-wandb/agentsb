# Provision fragment: install Claude Code.
# Not standalone; composed into lima/agentsb.yaml via `base:`.
provision:
  - mode: system
    script: |
      #!/usr/bin/env bash
      set -euxo pipefail
      npm install -g @anthropic-ai/claude-code
