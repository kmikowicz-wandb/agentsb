# agentsb base template — shared VM config for all agent templates.
# Per-agent templates in this directory inherit from this via `base:`.
#
# Security invariants that live here (don't override in children):
#   - Host $HOME is NOT mounted. Only WORKSPACE is visible inside the VM.
#   - ssh.loadDotSSHPubKeys: false — host SSH pubkeys don't leak into the VM.
#   - Egress firewall: pf rules on the macOS host (bridge100), not inside the VM.
#     Run `agentsb --setup-host` once to install them. See lima/pf-anchor.conf.
#   - Native ARM only (rosetta: false) for speed and to surface arch bugs early.

images:
  - location: "https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-arm64.img"
    arch: "aarch64"
  - location: "https://cloud-images.ubuntu.com/releases/24.04/release/ubuntu-24.04-server-cloudimg-amd64.img"
    arch: "x86_64"

vmType: "vz"
vmOpts:
  vz:
    rosetta:
      enabled: false
      binfmt: false

cpus: 2
memory: "4GiB"
disk: "20GiB"

param:
  WORKSPACE: "/tmp"

mounts:
  - location: "{{.Param.WORKSPACE}}"
    mountPoint: "/workspace"
    writable: true

mountType: "virtiofs"

portForwards: []

# socket_vmnet exposes bridge100 to the macOS pf firewall, so egress rules
# can be enforced on the host rather than inside the VM.
networks:
  - lima: shared

ssh:
  loadDotSSHPubKeys: false

containerd:
  user: false
  system: false

provision:
  # --- Base packages, Node.js, uv ----------------------------------------
  - mode: system
    script: |
      #!/usr/bin/env bash
      set -euxo pipefail
      export DEBIAN_FRONTEND=noninteractive

      apt-get update
      apt-get install -y --no-install-recommends \
        ca-certificates curl git build-essential \
        python3 python3-pip python3-venv \
        jq ripgrep less

      # Node.js 22 LTS from NodeSource — claude-code and codex both need it.
      curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
      apt-get install -y --no-install-recommends nodejs

      # uv system-wide, so per-user shims don't need to know individual paths.
      curl -LsSf https://astral.sh/uv/install.sh | env UV_INSTALL_DIR=/usr/local/bin sh

      apt-get clean
      rm -rf /var/lib/apt/lists/*

  - mode: user
    script: |
      #!/usr/bin/env bash
      set -euxo pipefail
      mkdir -p "$HOME/.claude" "$HOME/.cache" "$HOME/.npm"
