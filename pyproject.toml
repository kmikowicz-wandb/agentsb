[project]
name = "agentsb"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
